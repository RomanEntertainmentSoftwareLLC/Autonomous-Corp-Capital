- Added a canonical analytics layer (analytics_companies/evaluations/trade_facts/company_performance), updated ingestion + reporting tools to use it, and made leaderboard/manager reports aware of UNTESTED/TESTED_NO_TRADES states so empty companies look like "no data yet." wrote new helpers and query path adjustments.
- Extended TradeDecision/logging to capture strategy/account/fitness details, now ingest_results_to_db uses those canonical fields, and evaluation reports now show both lifecycle and evaluation states.
- Aggregated multi-symbol runs during ingestion (latest timestamps, summed account/realized/unrealized/trade counts, win rates, max drawdown) and stored comma-separated strategy lists plus a default "unknown" regime so canonical reports see every symbol.
- Ensured canonical ingestion now pulls per-symbol latest log rows (not just executed trades), aggregates company totals, marks evaluations symbol as MULTI when needed, and documents the canonical schema semantics/lifecycle placeholders.
