- Implemented new company-local roles (Bianca/CFO, Lucian/CEO, Bob/Low Tier Operations Worker) with personas, policies, agent registrations for companies 001-004, and routing logic plus prompts through tools/pam.py and tools/llm_client.py.
- Added lifecycle-aware roster management (tools/company_roster.py) that creates/archives employees automatically alongside company creation, cloning, retirement, and restoration; wired into create_company/clone_company/evaluate_lifecycle and introduced tools/company_lifecycle.py for retire/test-retire/restore/sync flows.
- Added background commands enabling direct agent questions (e.g., `python3 tools/pam.py --agent lucian_company_001 ...`).

- Stabilized ROLE_STRUCTURED_OUTPUT for all global SWE/QA/infra roles (Product Manager through Infrastructure) and rewrote Pam prompts/packets accordingly, then fixed SimpleLLMAdapter branches so QA falls back on engineering/test/review contexts while only keeping one Product Manager/Junior SWE branch and rerunning py_compile for the relevant tools.

- Added Mara (inspector_general) as the global Inspector General persona/policy/agent, wired oversight keywords into Pam, expanded structured outputs + LLM fallback to produce audit packets, and verified the watchdog questions through the CLI (four sample queries).

- Added Justine (constitutional_arbiter) as the Constitutional Arbiter persona/policy/agent, routed constitutional keywords through Pam, extended structured output + LLM fallback for judicial rulings, and verified the four oversight questions via the CLI.

- Added Owen (ombudsman) as the Ombudsman/Appeals Officer persona/policy/agent, expanded Pam/LLM routing for complaint triage, and validated the four appeal queries via the CLI.

- Installed Yam Yam (yam_yam) as Master CEO: wrote persona/policy, registered the agent, wired the Master CEO structured output + LLM branch, routed care through the OpenAI adapter, and exercised the new executive-direction prompts for the six supplied QA checks.

- Fixed roster-sync so only company-local agents tied to inactive companies are removed (globals stay intact) and verified the new job list after `python3 tools/company_lifecycle.py roster-sync`.
- Stopped direct replies from Bianca/Lucian/Bob auto-routing to themselves, ensured `handoff_to` now stays empty, and normalized role strings safely so Master Treasurer no longer appears as "Master Treasurerr."
- Loaded policies alongside personas (with `policy_description` metadata) so prompts now carry mission/restriction context, and rebuilt the packet builder to respect explicit `escalate_to` hints before falling back to the Yam Yam inbox.

- Step 1: Improved global context gathering in tools/agent_runtime.py so master/watchdog prompts receive aggregated company summaries instead of "unknown state" noise, including the new _collect_company_metadata helper and policy descriptions in prompts.

- Step 2/3/4/5/6/7: split Pam helpers into new agent_roles/context/reports modules, added agent_kind tracking and OpenAI routing, hardened config writes (with backups), gave Infrastructure explicit gate checks, added the automated smoke suite (with env-safe subprocess runs), and ran it successfully alongside the compile check.

- Phase 1 concert-grade pass: moved global/watchdog fallback logic into tools/global_watchdog_fallbacks.py, dispatched from SimpleLLMAdapter, and verified py_compile/smoke suite along with eight openai-free queries for master/watchdog roles.
