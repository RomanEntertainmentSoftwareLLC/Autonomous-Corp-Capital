- 2026-03-24: Worked a long patch/verification sequence on the live paper trading runtime.
- Current live runtime changes now in workspace:
  - tools/live_run.py only writes paper_decisions.jsonl for executed rows.
  - tools/live_run.py strategy.log top_ranked now excludes skipped HOLD/no-action rows.
  - tools/live_run.py build_live_candle now builds minute-bucketed in-memory candles from polled live snapshots, with pseudo fallback during warmup/unsafe cases.
  - tools/live_run.py default LIVE_RUN_POLL_SECONDS was lowered from 60 to 10 to increase intraminute observations.
  - tools/live_decision_engine.py enforces pattern-first doctrine: BUY/SELL must have real_ohlc + non-empty detected_patterns + pattern_confirmation.satisfied, otherwise HOLD.
  - tools/live_decision_engine.py also adds a bounded ML quality veto after doctrine: confirmed BUY is vetoed if model_score <= 0.55; confirmed SELL is vetoed if model_score >= 0.45.
- Verified repeatedly via read-only inspection and short in-memory checks that research bias (Orion) and company packet overrides remain downstream of decision construction.
- Important truth gap still present: candle_source="real_ohlc" currently refers to reconstructed minute-bucket bars from polled quote snapshots, not a true exchange-native OHLC feed.
