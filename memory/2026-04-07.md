# 2026-04-07

- OpenClaw file work in this environment should use the actual tool names exposed here: `read_file` and `write_file`.
- Jacob prefers direct, calm, competent replies and does not want help-desk style closers.
- When a workspace file read succeeds, report the actual contents rather than refusing or hedging.
- Avoid outputting raw JSON tool-call objects in normal conversation; speak in plain language.
- Hermes is the executive cognition / memory layer for ACC; Yam Yam is the Master CEO.
- Always verify a file after writing it before telling the user it exists or was created.
- When a user says a written file is missing, re-check the file on disk instead of assuming the write failed.
- During a pre-compaction memory flush, append only to the canonical daily memory file for that date; do not overwrite existing entries.
