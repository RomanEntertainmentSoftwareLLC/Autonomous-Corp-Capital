# 2026-04-08

- OpenClaw prompt builders/adapters assume `role` / `role_type` are strings; list-valued values can crash on `.strip()`. Normalize with `isinstance(x, str)` or `str(x)` before string methods.
- In this workspace, use `python3` for local syntax checks and scripts; `python` is not installed.
