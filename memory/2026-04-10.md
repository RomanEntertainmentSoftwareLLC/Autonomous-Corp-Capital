# 2026-04-10

- Shared RPG logic now lives in `tools/rpg_state.py` and supports the blended canonical schema, legacy RPG_STATE.md loading/migration, `format_rpg_summary`, `format_rpg_identity_line`, `format_rpg_motivation_block`, and `get_role_scorecard`.
- The RPG state migration path was exercised on real agent state files; the blended schema is intended to preserve legacy fields like `level`, `title`, and `sessions` while adding operational stats and derived `intelligence`.
- The actual OpenClaw snapshot path used by the user is `/opt/openclaw/.openclaw/openclaw.json`; `/opt/openclaw/.openclaw/workspace/openclaw.json` was the wrong target for that snapshot check.
- RPG self-awareness is injected read-only via `tools/agent_context.py` using the helpers from `tools/rpg_state.py`; it shows identity, level/title, and a short improvement note without self-awarding XP.
- A single role-specific scoring path has been used for the first grounded RPG reward/penalty wiring; the role selector and scorecard helpers live in `tools/rpg_state.py`.
