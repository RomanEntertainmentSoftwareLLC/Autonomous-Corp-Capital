# 2026-04-12

- Pre-compaction flushes should append durable notes only to memory/YYYY-MM-DD.md; treat MEMORY.md, SOUL.md, AGENTS.md, and similar reference files as read-only during the flush.
- Do not claim a requested file/report step is done unless the file was actually written or verified; keep report responses grounded in the real file state.
- live_run stop must verify the worker actually exited before reporting success; if graceful stop fails, escalate instead of claiming "stopped safely".
- In live paper runs, the first-bucket pseudo candle can suppress nonzero ranking_score and keep the WAIT-promotion fallback unreachable; the bootstrap deadzone lives upstream of promotion logic.
- The narrow live promotion fallback should stay tied to positive ranking_score and aligned evidence; zero-score or empty-evidence cases should remain WAIT.
- When verifying live bootstrap fixes, run long enough to cross a minute bucket boundary; short runs can miss the real_ohlc effect and undercount BUY/SELL activity.
- When a major patch/test loop starts failing or timing out, break the work into smaller babysteps and verify each step independently instead of pushing a chunky one-pass run.
|- For babystep workflows, use exact completion markers the user asked for (for example: `BABYSTEP 1 COMPLETE`) so progress is unambiguous.
|- Live-run deadzones can be caused by cold-start `last_price=None` plus a first candle with `open == close`, which leaves `ranking_score` at 0.0 until real movement arrives.
|- When a later BUY/SELL burst appears, do not assume the early deadzone is fixed; verify both the cold-start packets and the later wake-up packets.
|"